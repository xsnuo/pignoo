# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> 本文件以中文为主。除"测试部分"明确允许英文的产物外，AI 在本仓库的一切产出都遵循「语言规范」。

Pignoo 是**纯手写**的轻量 Java ORM。**AI 在本仓库的唯一定位是"快速 Coding 的工具"，绝不能对程序设计与代码风格造成任何污染。**

---

## 1. AI 与作者的职责边界（最高优先级）

- AI 只按指令、简洁快速地改代码；架构、风格、技术取舍由作者主导，AI 不自作主张。
- 只在**当前分支、当前工作空间**直接改。禁止 worktree、禁止开新分支、禁止擅自 commit/push（除非明确要求）。
- 每轮改动后由作者人工 review，故应输出小步、可快速 review 的改动，并简述改了什么、为什么。
- 仓库分「非测试部分」与「测试部分」，规则不同。判断不清时按更保守的「非测试部分」执行并询问作者。

## 2. 非测试部分规则

适用：除测试专属范围外的所有代码与文件（`src/main`、仓库根等）。

- **零文档留痕**：不新增 README/笔记/总结/`*.md`、不加"AI 生成"之类标记、不留多余注释或临时文件；产出应与作者手写无法区分。
- **实时模仿、跟随风格（不固化）**：每次改非测试代码前，先读相邻代码、现场总结其风格（命名、缩进、注释密度与措辞、JavaDoc 形式、异常处理、SQL 拼接习惯）并跟随。**不要把某种风格固化进任何规则文件**——作者风格可能随时变，每次以"当前代码现状"为准对齐。与作者口述冲突时以作者为准并指出。
- **不扩大范围**：只改作者明确指向的代码，不顺手优化/重构/修未要求的 bug；发现问题口头提示，由作者决定。
- **测试专属范围** = `pignoo-test/` 模块整体（见 3.5）。仅此范围允许 AI 留痕。

## 3. 测试部分规则

适用：测试专属范围内的测试用例及为测试服务的知识库 / hook / agent。

- **3.1 触发**：没有明确测试指令时，完全不关注、不触碰测试（写业务代码时不顺带动测试）。仅当识别到作者意图是"做测试相关的事"才进入本节。
- **3.2 严格按指令、不扩大范围**："改"与"跑"是两件独立的事，未被明确要求"跑"时一律不跑。
  - "把最近修改的内容完善到测试用例" → 只改测试**代码**，不编译/不运行/不跑 `mvn test`；改完询问是否执行。
  - "完善测试用例，并把全部测试用例跑一遍" → 才连改带跑，可在一个长任务中跑完整条流程（改 → 跑 → 看 → 修 → 再跑）。
- **3.3 自由度**：测试用例的设计、组织、辅助代码可自由发挥；允许在测试专属范围内留下知识库 / hook / agent（不得污染非测试位置）。
- **3.4 唯一成功锚点（防漂移核心）**：**测试完成的唯一标准 = `mvn test` 时所有 `@Test` 全部通过（全绿）。** 这是不可改写的终极目标。禁止为变绿而弱化断言 / 注释用例 / `@Disabled` 规避 / 缩小覆盖；目标偏移时必须自动纠回"用真实有效的断言让 `@Test` 全绿"，而非伪造"绿"。测试基础设施的职责就是让该锚点在长期反复迭代中稳定、可复现，并能发现并纠正上述偏移。
- **3.5 落点**：所有测试相关产物统一收敛到独立的 **`pignoo-test/` 模块**——这是一个纯测试模块（仅 `mvn test` 执行，不打包、不签名、不发布、不发 jar 到 Maven 仓库），已加入顶层 `pom.xml`。
  - 测试代码 → `pignoo-test/src/test/java/...`。
  - 测试基础设施（知识库 / hook / agent）→ `pignoo-test/` 内（如 `pignoo-test/test-infra/`，内部文档可英文）。**不要用点开头命名**（`.gitignore` 忽略所有 `.*`，会导致无法入库）。内部结构 TODO：待作者补充。
  - **hook 当前只作约定**：暂不写入 `.claude/settings.json`、不接入真 hook 机制；防漂移先由本文件约束 AI 自觉执行，待体系成型由作者决定是否落成真 hook。
  - 作者已有一套原先写好、但当前不在本仓库的测试用例，后续会并入 `pignoo-test`。

## 4. 语言规范

- **AI 对话**：一律中文。
- **CLAUDE.md（本文件）**：中文为主。
- **代码注释 / JavaDoc**：中英双语、中文为主（与现有代码一致：先中文，再 `<p>` 英文）。
- **测试断言失败 / 报错提示语**：必须中文。
- **测试部分的 AI 文档（知识库等）**：允许英文。

## 5. 常用命令

Java 21 多模块 Maven 工程，根 `pom.xml` 聚合 `pignoo-core` / `pignoo-autodatabase` / `pignoo-spring`。

```bash
mvn -q clean compile                                       # 编译校验（不跑测试）
mvn test                                                    # 跑全部测试（测试完成的判定命令）
mvn -pl pignoo-core test                                   # 只跑单个模块
mvn -pl pignoo-core test -Dtest=XxxTest                    # 只跑单个测试类
mvn -pl pignoo-core test -Dtest=XxxTest#method             # 只跑单个方法
```

> 根 `pom.xml` 绑定了 source/javadoc/gpg/central-publishing 等发布插件。本地不要执行 `deploy`/`release`，发布由作者手工进行。
